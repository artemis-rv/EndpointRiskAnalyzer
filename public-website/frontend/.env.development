# Committed development defaults. Contains NO secrets — only public endpoints
# that already exist in the developer's own machine.
VITE_API_BASE_URL=http://localhost:8080
VITE_SITE_URL=http://localhost:3000
VITE_API_TIMEOUT_MS=15000
VITE_SUPPORT_EMAIL=support@riskintel.example.com
VITE_ENABLE_DEV_DIAGNOSTICS=true
